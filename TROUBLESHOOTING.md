# 🔧 Решение проблем - Сообщения не приходят

## ✅ Пошаговая проверка

### Шаг 1: Проверьте токен бота

```bash
cd bot
cat .env
```

**Должно быть:**
```env
BOT_TOKEN=1234567890:ABC... (настоящий токен)
ADMIN_GROUP_ID=-3141977295
```

**НЕ должно быть:**
```env
BOT_TOKEN=your_bot_token_here  ❌ ЭТО НЕПРАВИЛЬНО!
```

**Как получить токен:**
1. @BotFather → `/mybots`
2. Выберите @Jan_ny2026_bot
3. Нажмите "API Token"
4. Скопируйте токен
5. Вставьте в `bot/.env`

📖 Подробно: [SETUP_BOT_TOKEN.md](./SETUP_BOT_TOKEN.md)

---

### Шаг 2: Установите зависимости

```bash
cd bot
npm install
```

Должно установить:
- node-telegram-bot-api
- express
- cors
- dotenv

---

### Шаг 3: Запустите бота

```bash
cd bot
npm start
```

**Должно появиться:**
```
==================================================
🤖 Telegram Bot запущен успешно!
==================================================
📱 WebApp URL: https://...
🌐 API сервер: http://localhost:3000
📨 Группа админов: -3141977295
==================================================
✅ Бот готов принимать заказы!
📖 Откройте @Jan_ny2026_bot и отправьте /start
==================================================
```

**Если видите ошибки:**

❌ **"BOT_TOKEN не настроен"**
→ Вставьте токен в `bot/.env`

❌ **"401 Unauthorized"**
→ Токен неправильный, получите новый у @BotFather

❌ **"Cannot find module"**
→ Запустите `npm install` в папке bot/

❌ **"node: command not found"**
→ Установите Node.js: https://nodejs.org/

---

### Шаг 4: Проверьте бота в Telegram

Откройте @Jan_ny2026_bot и отправьте:
```
/start
```

**Должно появиться:**
```
🎄 Привет, Ваше_Имя! С наступающим Новым Годом! 🎊
...
[🎁 Сделать заказ] кнопка
```

**В консоли бота должно быть:**
```
👤 /start от: Ваше_Имя (ID: 123456789)
✅ Приветствие отправлено
```

**Если бот не отвечает:**
- Проверьте что `npm start` работает
- Проверьте что нет ошибок в консоли
- Перезапустите бота (Ctrl+C, потом npm start)

---

### Шаг 5: Добавьте бота в группу

1. Откройте группу: https://web.telegram.org/k/#-3141977295
2. Нажмите ⋮ → "Add members"
3. Найдите @Jan_ny2026_bot
4. Добавьте бота
5. **ВАЖНО:** Дайте боту права администратора!
   - Нажмите на имя бота в группе
   - ⋮ → "Promote to Admin"
   - Включите "Post messages"
   - Сохраните

**Проверка:** Бот должен появиться в списке администраторов группы

---

### Шаг 6: Проверьте отправку заказа

**ВАЖНО:** Пока приложение не задеплоено, кнопка "🎁 Сделать заказ" работать не будет!

#### Для локального тестирования:

1. Откройте http://localhost:5177/
2. Заполните форму заказа
3. Нажмите "Отправить заказ"
4. Смотрите консоль браузера (F12)

**Должно быть в консоли:**
```
Отправка данных в бот: {...}
```

**В консоли бота должно быть:**
```
📦 Получены данные из WebApp
📋 Данные заказа: {...}
🔢 Номер заказа: NY123456
📤 Отправка клиенту...
✅ Сообщение клиенту отправлено
📤 Отправка в группу -3141977295
✅ Сообщение в группу отправлено
🎉 Заказ NY123456 успешно обработан!
```

---

## 🔍 Диагностика по логам

### Бот запущен, но не отвечает на /start

**Проверьте:**
```bash
ps aux | grep node
```

Должен быть процесс: `node server.js`

**Перезапустите:**
```bash
# Ctrl+C в терминале с ботом
npm start
```

---

### Кнопка "Сделать заказ" не работает

**Причина:** WebApp URL не настроен

**Решение:**
1. Задеплойте приложение на Vercel/Netlify
2. Обновите WEBAPP_URL в `bot/.env`
3. Привяжите WebApp в @BotFather:
   ```
   /newapp → @Jan_ny2026_bot
   URL: ваш_vercel_url
   ```

---

### Заказ отправляется, но сообщения не приходят

**Проверьте консоль бота:**

✅ Должно быть:
```
📦 Получены данные из WebApp
✅ Сообщение клиенту отправлено
✅ Сообщение в группу отправлено
```

❌ Если видите ошибки:

**"Chat not found"**
→ Бот не добавлен в группу или неправильный ID

**"Need administrator rights"**
→ Дайте боту права администратора в группе

**"Can't parse entities"**
→ Проблема с форматированием HTML, проверьте данные заказа

---

### В группу сообщения не приходят

**Проверка 1: ID группы**
```bash
cat bot/.env | grep ADMIN_GROUP_ID
```

Должно быть: `ADMIN_GROUP_ID=-3141977295`
(Обратите внимание на минус `-` в начале!)

**Проверка 2: Бот в группе?**
Откройте группу → Участники → должен быть @Jan_ny2026_bot

**Проверка 3: Права администратора**
@Jan_ny2026_bot → должна быть пометка "admin"

**Проверка 4: Тестовое сообщение**
В консоли Node.js выполните:
```javascript
bot.sendMessage('-3141977295', 'Тест')
  .then(() => console.log('OK'))
  .catch(err => console.log('Ошибка:', err.message));
```

---

## 🧪 Тест полного цикла

### 1. Бот запущен:
```bash
cd bot
npm start
```

### 2. Frontend запущен:
```bash
npm run dev
```

### 3. Откройте в браузере:
```
http://localhost:5177/
```

### 4. Заполните форму и отправьте

### 5. Проверьте логи в консоли бота:
```
📦 Получены данные из WebApp
От: Test User
📋 Данные заказа: {...}
🔢 Номер заказа: NY123456
📤 Отправка клиенту...
✅ Сообщение клиенту отправлено
📤 Отправка в группу -3141977295
✅ Сообщение в группу отправлено
🎉 Заказ NY123456 успешно обработан!
```

---

## 📞 Если ничего не помогло

### Проверьте версии:

```bash
node --version  # Должно быть >= 16
npm --version   # Должно быть >= 8
```

### Очистите и переустановите:

```bash
cd bot
rm -rf node_modules package-lock.json
npm install
npm start
```

### Проверьте файрвол:

Убедитесь что порт 3000 не заблокирован

### Логи в файл:

```bash
npm start > bot.log 2>&1
cat bot.log
```

---

## ✅ Чеклист "Всё работает"

- [ ] `bot/.env` содержит правильный токен
- [ ] `npm install` выполнен в папке bot/
- [ ] `npm start` запущен без ошибок
- [ ] Бот отвечает на /start в Telegram
- [ ] Бот добавлен в группу -3141977295
- [ ] Боту даны права администратора
- [ ] В консоли бота видны логи при отправке заказа
- [ ] Сообщения приходят клиенту и в группу

**Если все пункты выполнены - всё должно работать!** ✅

---

## 📚 Полезные ссылки

- [SETUP_BOT_TOKEN.md](./SETUP_BOT_TOKEN.md) - Как получить токен
- [QUICK_START.md](./QUICK_START.md) - Быстрый старт
- [BOT_SETUP.md](./BOT_SETUP.md) - Полная настройка
- [MESSAGE_EXAMPLES.md](./MESSAGE_EXAMPLES.md) - Примеры сообщений

🎄 Удачи! 🎊
