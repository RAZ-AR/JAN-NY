# Быстрый старт

## За 5 минут до первого заказа

### 1. Клонируйте и установите зависимости

```bash
git clone https://github.com/username/ny-sets-miniapp.git
cd ny-sets-miniapp
npm install
```

### 2. Запустите локально

```bash
npm run dev
```

Откройте http://localhost:5173/ в браузере

### 3. Создайте Telegram бота

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot`
3. Придумайте имя: "Новогодние Сеты"
4. Придумайте username: `ny_sets_2024_bot`
5. Сохраните токен

### 4. Задеплойте на Vercel (рекомендуется)

```bash
npm install -g vercel
vercel login
vercel --prod
```

Получите URL: `https://your-project.vercel.app`

**Альтернатива**: GitHub Pages, Netlify, Cloudflare Pages (см. [DEPLOYMENT.md](DEPLOYMENT.md))

### 5. Настройте Web App в боте

1. В BotFather отправьте `/newapp`
2. Выберите своего бота
3. Название: "Новогодние сеты"
4. Описание: "Закажите сеты на Новый Год"
5. Загрузите фото (640x640px минимум)
6. URL: `https://your-project.vercel.app`

### 6. Настройте бота для приема заказов

```bash
# В папке проекта
npm install node-telegram-bot-api dotenv
```

Создайте файл `.env`:
```
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_CHAT_ID=ваш_telegram_id
WEB_APP_URL=https://your-project.vercel.app
```

Скопируйте `bot-example.js` в `bot.js`:
```bash
cp bot-example.js bot.js
```

Запустите бота:
```bash
node bot.js
```

### 7. Получите свой Telegram ID

1. Откройте [@userinfobot](https://t.me/userinfobot)
2. Нажмите Start
3. Скопируйте ваш ID
4. Вставьте в `.env` как `ADMIN_CHAT_ID`

### 8. Тестируйте!

1. Откройте вашего бота в Telegram
2. Нажмите `/start`
3. Нажмите кнопку "Заказать сеты"
4. Заполните форму заказа
5. Проверьте, что заказ пришел вам в Telegram

---

## Готово! 🎉

Теперь ваше мини-приложение работает и принимает заказы.

## Следующие шаги

### Настройка внешнего вида
Измените цвета градиента в `src/App.css`:
```css
background: linear-gradient(
  180deg,
  #8B9E7D 0%,    /* Измените эти цвета */
  #E89B7E 20%,
  #F5B885 40%,
  #FFD08A 60%,
  #B084CC 80%,
  #E89B7E 100%
);
```

### Настройка сетов
Измените данные в `src/data/sets.js`:
```javascript
export const sets = [
  {
    id: 1,
    name: 'СЕТ НА 2Х',
    price: 6000,  // Измените цены
    weight: 2300,
    people: 2,
    items: [...]  // Измените состав
  },
  // ...
];
```

### Добавление промокодов
В `bot.js` добавьте проверку промокодов:
```javascript
const PROMO_CODES = {
  'NEWYEAR2024': 10, // 10% скидка
  'HAPPY2024': 500    // 500 дин скидка
};

function applyPromoCode(totalPrice, promoCode) {
  if (!promoCode) return totalPrice;

  const discount = PROMO_CODES[promoCode.toUpperCase()];
  if (!discount) return totalPrice;

  // Процентная скидка
  if (discount < 100) {
    return totalPrice * (1 - discount / 100);
  }
  // Фиксированная скидка
  return Math.max(0, totalPrice - discount);
}
```

### Кастомизация сообщений
Измените тексты в компонентах:
- `UserInfoStep.jsx` - заголовок и подзаголовок
- `SuccessStep.jsx` - сообщение об успехе
- `bot.js` - форматирование сообщений админу

---

## Важные файлы

| Файл | Описание |
|------|----------|
| `src/App.jsx` | Главная логика приложения |
| `src/data/sets.js` | Данные о сетах и слотах |
| `bot.js` | Обработчик заказов |
| `.env` | Настройки бота (не коммитить!) |

---

## Частые проблемы

### Бот не получает заказы
- Проверьте, что бот запущен (`node bot.js`)
- Проверьте токен в `.env`
- Проверьте, что URL в BotFather правильный

### Приложение не открывается в Telegram
- Проверьте, что URL деплоя работает в браузере
- Проверьте HTTPS (обязательно!)
- Убедитесь, что URL указан в BotFather

### Градиенты не работают
- Hard refresh (Ctrl+Shift+R)
- Проверьте поддержку браузера
- Откройте в другом браузере

---

## Полезные команды

```bash
# Разработка
npm run dev

# Сборка
npm run build

# Предпросмотр сборки
npm run preview

# Запуск бота
node bot.js

# Деплой на Vercel
vercel --prod

# Проверка логов бота
pm2 logs ny-sets-bot
```

---

## Поддержка

- 📖 [README.md](README.md) - полная документация
- 🤖 [TELEGRAM_BOT_SETUP.md](TELEGRAM_BOT_SETUP.md) - настройка бота
- 🚀 [DEPLOYMENT.md](DEPLOYMENT.md) - деплой на различные платформы
- 🏗️ [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) - структура проекта

---

## Чеклист готовности к продакшену

- [ ] Бот создан в BotFather
- [ ] Приложение задеплоено на HTTPS
- [ ] Web App настроен в боте
- [ ] Бот запущен и принимает заказы
- [ ] Тестовый заказ прошел успешно
- [ ] Настроены цены и состав сетов
- [ ] Установлены правильные слоты доставки
- [ ] Проверена работа на мобильном
- [ ] Настроена аналитика (опционально)
- [ ] Создан бэкап проекта

С наступающим Новым Годом! 🎄
